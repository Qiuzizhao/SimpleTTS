# 简单 TTS 方案：不方便开口患者的文字朗读工具

> 版本：v0.2（已采纳 edge-tts 优选版）｜适用：刚拔牙等术后不便开口的患者，向医护/家属表达需求

---

## 1. 目标

- 患者**打字或点按短语**，设备把文字**朗读**出来，完成面对面沟通。
- 中文（普通话）即可。
- **网页版**：浏览器打开即用，手机 / 平板 / 电脑通用，无需安装。
- 语音以 **微软神经语音"晓伊"** 为主（音质自然），**断网自动降级**为浏览器本地语音。

## 2. 核心选型

| 方案 | 说明 | 结论 |
|---|---|---|
| **B. 本地 Python 服务 + edge-tts**（主通道） | 微软 Edge 在线 TTS（免费、无需注册），中文"晓伊"神经语音，音质接近真人；需联网、需装 Python | ✅ **主语音** |
| **A. 浏览器 Web Speech API**（兜底） | 纯本地合成，断网可用，音质较机械 | ✅ **离线兜底**（服务不可用时自动切换） |
| C. 完全离线本地模型（Piper） | 离线+音质好；但部署重、模型大 | 暂不采用 |

**组合逻辑**：有网络 → 走 edge-tts 晓伊（好音质）；服务没起/断网 → 页面自动改用浏览器自带语音，并提示"已降级为本地语音"。两层都有，功能上永远可用。

## 3. 系统架构

```
┌─────────────── 病房电脑 / 平板 ───────────────┐
│                                               │
│  浏览器页面 (index.html)                       │
│    ├─ 有网/服务正常 → 请求 /api/tts ──┐        │
│    └─ 失败(离线) → speechSynthesis 兜底│        │
│                                      ▼        │
│                          本地 Python 服务      │
│                          (FastAPI, :8000)      │
│                            │ edge-tts         │
└────────────────────────────┼──────────────────┘
                             ▼
              微软 Edge TTS 在线语音（互联网）
              （免费，无需 API Key）
```

- 页面和语音服务**同机同端口**：患者只需访问 `http://localhost:8000`（或局域网 IP）。
- 服务内置**语音缓存**：同一句话第二次朗读秒出（快捷短语重复点击零等待）。
- 文字仅发往微软官方 TTS 接口，不落任何第三方存储。

## 4. 功能设计（MVP）

1. **大输入框** —— 输入任意文字。
2. **大"朗读"按钮** —— 整块可点，点击即读。
3. **快捷短语区** —— 口腔术后高频需求一键朗读；**支持页面内增删（编辑模式、删除可撤销）**，保存在**服务端**（多设备同步），本机留离线备份（见 §7）。
4. **停止朗读**；朗读中高亮当前短语。
5. **语速 / 音量** 调节滑块（照顾老年/恢复期患者）。
6. **连接状态角标**：动态显示当前音色（如"晓伊语音 ✓ / 已降级为本地语音"）。
7. **大字号、高对比、触屏友好**。
8. **常用语音频缓存**：服务端磁盘缓存（重启不丢）+ 浏览器长缓存头 + 打开页面时后台预热常用语，重复朗读秒出。

## 5. 本地服务 API 设计

| 接口 | 说明 |
|---|---|
| `GET /` | 返回页面（前后端同源，一个服务搞定） |
| `GET /api/tts?text=…&voice=…&rate=…&volume=…` | 返回 `audio/mpeg` 音频流（边合成边播放）；音频按 URL 缓存到 `data/cache/` 目录并带浏览器长缓存头，重复朗读秒出 |
| `GET /api/voices` | 返回可用中文语音列表（默认 `zh-CN-XiaoyiNeural` 晓伊） |
| `GET /api/phrases` | 读取常用短语（服务端持久化） |
| `PUT /api/phrases` | 保存常用短语（整表替换，单用户场景足够） |
| `GET /api/ping` | 健康检查（页面据此决定是否降级） |

- 文本走 URL 编码（GET），常规短句无压力；服务端按 `文本+语音+参数` 哈希做内存缓存。
- 技术栈：`FastAPI + uvicorn + edge-tts`（三个 pip 包，无其他依赖）。

## 6. 界面草图

```
┌──────────────────────────────────────┐
│  不方便开口？打字，我来帮你说话   [晓伊✓] │
├──────────────────────────────────────┤
│  ┌────────────────────────────────┐  │
│  │  （大输入框，placeholder：请输入想说的话…）│  │
│  └────────────────────────────────┘  │
│        [ 🔊 朗读 ]   [ ■ 停止 ]       │
│  ────────────────────────────────    │
│  [我想喝水] [我疼] [叫护士] [谢谢]     │
│  [我要上厕所] [冰敷] [头晕] [想回家]   │
│  ────────────────────────────────    │
│  语速 ───●───   音量 ───●───          │
└──────────────────────────────────────┘
```

## 7. 快捷短语（口腔术后场景初稿，改 `phrases.js` 即可）

```
[我想喝水] [我疼 / 需要止痛药] [叫护士] [我要上厕所] [谢谢]
[太冷了] [太热了] [冰敷一下] [我有点头晕] [我恶心/想吐] [我饿了]
[我想回家] [医生，我有问题] [听不清，请再说一遍] [需要纸巾] [想睡觉]
```

## 8. 目录结构

```
SimpleTTS/
├── index.html      # 页面结构
├── style.css       # 大字号 / 高对比 / 触屏样式
├── app.js          # 语音播放、降级逻辑、本地存储
├── phrases.js      # 快捷短语配置（最常改动）
├── server.py       # FastAPI 服务：静态页 + /api/tts + 短语 + 缓存
├── data/           # 数据目录：语音缓存 + phrases.json（自动生成，可挂载持久化）
├── requirements.txt# fastapi / uvicorn / edge-tts
├── start.bat       # 一键启动：装依赖→起服务→开浏览器
├── start-lan.bat   # 局域网模式：手机/平板访问（打印局域网地址）
└── 方案.md         # 本文档
```

## 9. 实施步骤（预计 1~1.5 天出可用版）

1. `server.py`：FastAPI 静态页 + `/api/tts`（edge-tts 流式输出）+ 语音缓存 —— ~2 小时
2. 页面骨架：输入框 + 朗读/停止 + 短语区 —— ~30 分钟
3. 前端播放与降级逻辑：`/api/tts` 流式播放，失败自动切 speechSynthesis —— ~1 小时
4. 快捷短语渲染 + 点击朗读 + 状态高亮 —— ~40 分钟
5. 语速/音量 + 自定义短语 localStorage —— ~1 小时
6. `start.bat` / `start-lan.bat` 一键脚本 —— ~30 分钟
7. 真机验证：电脑 Chrome、手机浏览器、拔网线验证降级 —— ~1 小时

## 10. 验收标准

- 联网时：输入中文点"朗读"，**晓伊**音质，1 秒内出声；快捷短语重复点击秒出（缓存）。
- 断网/服务未启动：页面自动降级为浏览器语音，仍可朗读，并显示降级提示。
- 手机浏览器（同局域网）访问可用，按钮够大、能点准。

## 11. 后续升级（按需再议）

- **男声/多音色**：`/api/voices` 已预留，界面加"音色"选择（云希男声等）。
- 完全离线 + 好音质：换本地模型（Piper），部署较重，暂缓。
- 慢速模式一键切换、常用语联想、语音播报后自动清空。

## 12. 风险与对策

| 风险 | 对策 |
|---|---|
| 部署到公网无鉴权 | 建议放 HTTPS 反向代理（Caddy/Nginx）后面；需要访问令牌可再加一层 |
| 病房电脑没装 Python | `start.bat` 检测并提示安装（Python 3.9+，一次性） |
| edge-tts 需联网，断网无晓伊 | 自动降级浏览器语音，功能不中断 |
| 首次启动防火墙弹窗 | `start-lan.bat` 提示允许；纯本机用不受影响 |
| 文本过长（URL 超长） | 常规短句无碍；预留 POST 方案，需要时切换 |
| 微软接口变动 | edge-tts 社区活跃，升级包即可；浏览器兜底始终可用 |

---
*确认后即按第 9 节开始实现。有想调整的点（短语、界面、音色）直接说。*
